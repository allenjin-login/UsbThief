# Version information - filtered by Maven during build
# Do not edit manually - version is managed in pom.xml
version.major=${project.version.major}
version.minor=${project.version.minor}
version.patch=${project.version.patch}
version.build=${build.timestamp}
